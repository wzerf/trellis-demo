/**
 * @deprecated 精简过程中被裁掉的模块。保留为 stub 以维持被引用页面的可编译性。
 * 任何运行时调用会直接抛错（页面未注册路由，正常情况下不会执行）。
 */
export const __deprecated = true;
export function fetchMyPermissionCode() {
  throw new Error('[admin-portal] 模块已废弃，不可用');
}
